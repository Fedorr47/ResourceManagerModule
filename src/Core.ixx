export module core;

// Re-export core subsystems.
// If you already have a primary `core` module interface unit in your project,
// merge the `export import` lines below into it instead of keeping this file.

export import :resource_manager;